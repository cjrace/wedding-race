# Set default behaviour incase anyone doesn't have the same defaults
* text eol=lf

# Declare special files that shouldn't be forced to LF line endings
wedding-race-frontend/public/* -text
wedding-race-frontend/public/images/* -text